// STRING, NUMBER, UNDEFINED, NULL, BOOLEAN, SYMBOL

const nome = 'Luiz'; //string
const nome1 = 'Bianca' //string

const num1 = 19; //number
const num2 = 17; //number

let numeFilho; //undefined ->não aponta para local nenhum da memoria
let sobrenomeFilho = null; //nulo -> não aponta para um local na memoria

const aprovado = true; //boolean -> ou true ou false
const reprovado = false; //boolean -> ou true ou false


let nota1 = 8
let nota2 = 6
const ex = (nota1 + nota2) / 2
if(ex >= 7){
    media = 'aprovado'
    if(media = 'aprovado'){
        mediaN = true
    }
}
console.log(`com sua média de ${ex}, você foi ${media}`)
console.log(typeof mediaN)  